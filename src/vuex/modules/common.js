const state = {
  // alert: 666,
}

const getters = {
  // alert: state => state.alert,
}

const mutations = {
    // mutation: (state, data) => {

    // },
}

const action = {
  // logoutRequest: ({commit, dispatch}) => {

  // }
}

export default {
    state,
    getters,
    mutations,
    action
}
